// StrategyCanvasWorkspaceView.swift — placeholder for Task 18
// 真正的实现在 Phase 8（HUD + 状态栏 + 6 浮层面板）。
import SwiftUI

struct StrategyCanvasWorkspaceView: View {
    var body: some View {
        Text("workspace placeholder")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
