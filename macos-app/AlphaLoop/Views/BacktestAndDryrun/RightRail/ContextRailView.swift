// ContextRailView.swift — Right rail: context inspector (filled in Task 10).

import SwiftUI

struct ContextRailView: View {
    var body: some View {
        VStack {
            Text("Context Rail")
        }
    }
}
